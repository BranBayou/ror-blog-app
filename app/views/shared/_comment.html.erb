<ul class='comment-card'>
<% comments.each do |comment|%>
<li class='comment-li'>
<span><%= User.find(comment.author_id).name %>: <%= comment.text %></span>
</li>
<%end%>
<%= 'No comments' if comments.empty?%>
</ul>